<UserControl x:Class="MyWmp.View.OpenStream"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300" IsVisibleChanged="OpenStream_OnIsVisibleChanged">
    <Popup IsOpen="false" x:Name="Popop" MaxHeight="50" MaxWidth="270" Height="50" Width="270">
        <StackPanel Orientation="Horizontal" Background="{DynamicResource BgBrush}">
            <TextBox x:Name="StreamUrl" Text="http://" Style="{DynamicResource Textbox}" Height="20" Width="200" Margin="10,0,10,0"/>
            <Button x:Name="ConfirmStream" Content="/Resources/Play.png" Style="{DynamicResource Controls}" Click="ConfirmStream_OnClick"/>
        </StackPanel>
    </Popup>
</UserControl>
