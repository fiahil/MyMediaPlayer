<UserControl x:Class="MyWmp.View.Theme"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="50" d:DesignWidth="100" IsVisibleChanged="Theme_OnIsVisibleChanged">

    <Popup x:Name="PopupTheme" Width="100" Height="50" IsOpen="True">
        <Grid>
            <StackPanel Orientation="Vertical" Background="Red">
                <GroupBox x:Name="Background" Header="Background">
                    <Button Content="Background" />
                </GroupBox>
            </StackPanel>
        </Grid>
    </Popup>
</UserControl>
