<UserControl x:Class="MyWmp.View.PlaybackList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" xmlns:viewModel="clr-namespace:MyWmp.ViewModel"
      d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.DataContext>
        <viewModel:PlaybackListViewModel/>
    </UserControl.DataContext>
    
    <StackPanel Orientation="Vertical" Background="White">
        <TextBlock Text="{Binding Name}"/>
        <ListView ItemsSource="{Binding Playlist}">
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderContainerStyle>
                        <Style>
                            <Setter Property="FrameworkElement.Visibility" Value="Visible"/>
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>
                    <GridViewColumn DisplayMemberBinding="{Binding Title}"></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Duration}"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
